# Steps to push existing folder into git :

* git init
* git remote add origin https://github.com/github_username/repo_name.git
* git branch -M branch_name
* commit the changes
* git push -u origin branch_name